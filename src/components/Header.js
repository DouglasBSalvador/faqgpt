import React from 'react';

const Header = () => {
    return (
        <header style={{ padding: '10px', background: '#3a5bc6', color: '#ededed', textAlign: 'center' }}>
            <h1>FaqGPT</h1>
        </header>
    );
};

export default Header;